CREATE TABLE cycles (
_id INTEGER NOT NULL PRIMARY KEY
,_name TEXT NOT NULL UNIQUE
,_regex TEXT NOT NULL
,_official BOOLEAN NOT NULL
,_category TEXT NOT NULL
,_synergy TEXT NOT NULL
,_typed BOOLEAN NOT NULL
,_fetch BOOLEAN NOT NULL
,_ramp BOOLEAN NOT NULL
);

INSERT INTO cycles(_id, _name, _regex, _official, _category, _synergy, _typed, _fetch, _ramp)
VALUES(1, "misc", "NA", False, "undefined", "None", False, False, False);

