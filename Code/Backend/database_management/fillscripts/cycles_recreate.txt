CREATE TABLE cycles (
_id INTEGER NOT NULL PRIMARY KEY
_name TEXT NOT NULL UNIQUE
_regex TEXT NOT NULL
_official BOOLEAN NOT NULL
_category TEXT NOT NULL
);

INSERT INTO cycles(_id, _name, _regex, _official, _category)
VALUES(1, "misc", "NA", False, "undefined")
;

emerging questions:
format the _regex stuff
