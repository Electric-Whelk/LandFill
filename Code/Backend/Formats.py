from Format import Format

format_dict = {
    "standard": Format("standard", 60, False, False),
    "modern": Format("modern", 60, False, False),
    "legacy": Format("legacy", 60, False, False),
    "EDH": Format("EDH", 100, True, True)
}